# Upgrade your current repository to Phase 2

1. Stop the running server with `Ctrl + C`.
2. Upload and unzip this package.
3. Copy all files into the root of `/workspaces/philip-kaloki-2027`.
4. Run:

```bash
npm install
npm run dev
```

The project should open on port 5173.
