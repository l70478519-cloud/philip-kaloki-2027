# Phase 28 — Functional Links & Public Forms

- Candidate logo links to Home.
- Menu and CTA links remain real routes.
- Phone links use `tel:`.
- Email links use `mailto:`.
- WhatsApp uses a valid `wa.me` link.
- Social links are normalized to HTTPS destinations.
- Volunteer form now returns the actual API error instead of incorrectly saying the API server is not running.
- Volunteer form validates required fields and consent.
- Public form endpoint uses clearer validation and responses.
- Admin View Website is a real link.
- No Supabase migration is required.
